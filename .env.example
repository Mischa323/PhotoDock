# Your domain name — Caddy gets an SSL certificate automatically for this domain
# Make sure your domain's DNS points to this server and ports 80/443 are open
DOMAIN=your-domain.com

# Port the app listens on inside the container (default: 8080, no need to change)
PORT=8080
